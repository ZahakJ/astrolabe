// Types for scripts/shell-seam.mjs, so tests/phoneShell.test.ts can import it.
export function importsOf(text: string): string[];
export function widthQueries(css: string): string[];
export function seamViolations(root: string): string[];
