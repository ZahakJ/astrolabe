<?xml version="1.0" encoding="utf-8"?>
<!-- The default room's ground, accent and ink, straight out of client/styles/tokens.css `:root` (github-dark)
     :root. The window wears them so the system bars, the letterbox behind the
     WebView and the moment before the first paint are all the same colour as the
     page — not a white flash with a dark app arriving late. -->
<resources>
    <color name="ground">#0d1117</color>
    <color name="accent">#58a6ff</color>
    <color name="ink">#e6edf3</color>
</resources>
